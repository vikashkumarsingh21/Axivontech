const fs = require('fs'); let c = fs.readFileSync('prisma/schema.prisma', 'utf8'); c = c.replace('users     User[]', 'users     User[]\n  portfolioProjects PortfolioProject[]'); c = c.replace('ownedClients       CrmClient[]    @relation('ClientOwner')', 'ownedClients       CrmClient[]    @relation('ClientOwner')\n  createdPortfolioProjects PortfolioProject[] @relation('PortfolioCreator')'); fs.writeFileSync('prisma/schema.prisma', c);
